# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          VITE_PRISMIC_REPO: ${{ secrets.VITE_PRISMIC_REPO || vars.VITE_PRISMIC_REPO }}
          VITE_PRISMIC_ACCESS_TOKEN: ${{ secrets.VITE_PRISMIC_ACCESS_TOKEN || vars.VITE_PRISMIC_ACCESS_TOKEN }}
          VITE_VAPID_PUBLIC_KEY: ${{ secrets.VITE_VAPID_PUBLIC_KEY || vars.VITE_VAPID_PUBLIC_KEY }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CATHOLIC_PRAYER_E6872 }}
          channelId: live
          projectId: catholic-prayer-e6872
